# Beroenden för edge/ (PIR→kamera). Hålls separat från inference/requirements.txt
# för att inte dra in onödiga paket på Pi 3B+.
#
# gpiozero = enkla klasser ovanpå GPIO-pinnarna. Har en inbyggd MockFactory
# med låtsas-pinnar, vilket gör att koden går att testa på Mac utan hårdvara.
gpiozero==2.0.1

# Test-only, men vi håller allt i en fil eftersom projektet är litet.
pytest==8.4.2
