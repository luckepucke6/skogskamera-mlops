# Beroenden för edge/-testerna på Mac. Hålls separat från inference/requirements.txt.
#
# På Pi:n installeras numpy/Pillow via apt-paketet python3-picamera2 istället —
# den här filen är bara för Mac (ingen kamerahårdvara här).
numpy==2.5.2
pillow==12.3.0
pytest==8.4.2

# POST:ar bilden till inferens-tjänsten. Finns redan som apt-paket (python3-requests) på Pi:n.
requests==2.34.2
