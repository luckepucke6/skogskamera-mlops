# Beroenden för att köra inferens lokalt.
#
# ai-edge-litert = TFLite-interpretern. Vi använder inte fulla tensorflow
# (flera GB) — den här är Googles lättviktspaket, samma vi vill ha på Pi 3B+.
ai-edge-litert==2.2.0
numpy==2.5.2
pillow==12.3.0

# FastAPI-tjänsten (app.py) + dess loggnings-sinks.
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
prometheus-client==0.26.0
mlflow-skinny==3.16.0
requests==2.34.2

# Test-only (inference/test_app.py).
httpx==0.28.1
pytest==8.4.2
